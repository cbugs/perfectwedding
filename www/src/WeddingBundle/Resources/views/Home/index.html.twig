{% extends 'WeddingBundle:Regions:base.html.twig' %}

{% block title %}Homepage{% endblock %}

{% block body %}
  
  {{ render(controller('WeddingBundle:Home:headerSearch')) }}
  
  {{ render(controller('WeddingBundle:Home:popularCategories')) }}
  
  {{ render(controller('WeddingBundle:Home:planningTools')) }}
  
  {{ render(controller('WeddingBundle:Home:featuredWeddingVendor')) }}

{#
  {{ render(controller('WeddingBundle:Home:testimonials')) }}

  {{ render(controller('WeddingBundle:Home:topWeddingLocations')) }}
#}
  
{% endblock %}

